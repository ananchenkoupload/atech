$color-theme-primary:	#BA9163;
$cart-text-color:		#212121;
$cart-border-color:		$cart-text-color;

#PageContainer{
/*   max-width: calc(1166px + 30px); */
/*   margin:0; */
}

#shopify-section-cart-template{
  padding: 47px 0 0;
  .page-width{
    max-width: calc(1166px + 30px);
    padding-right:15px !important;
    padding-left:15px !important;
/*     margin:0 !important; */
  }
}

.cart-header{
  color: $cart-text-color;
  font-size: 16px;
  line-height: 1.3077;
  margin: 0 0 11px;
  text-align: left;
  .text-link--accent{
    border: 0;
    color: $color-theme-primary;
    font-size: inherit;
    font-weight: inherit;
    letter-spacing: 0;
  	line-height: inherit;
    padding: 0;
    text-decoration: none;
    text-transform: capitalize;
  }
}
.cart-header__title{
  color: $cart-text-color;
  font-size: 26px;
  font-weight: normal;
  line-height: 1.375;
  letter-spacing: 0.0133em;
  margin: 0 0 11px;
  text-transform: uppercase;
}

form.cart {
  display: flex;
  flex-direction: row;
  flex-wrap: nowrap;
  justify-content: flex-start;
  align-content: flex-start;
  align-items: flex-start;
  font-size: 14px;
  line-height: 1.5;
  margin: 0 -34px;
  > .column{
    flex: 1 1 auto;
    padding: 0 34px 34px;
    &.cart-table{
      min-width: 866px;
    }
  }
}

.cart-table{
  table{
    margin: 0;
  }
}

.cart{
  th,
  td{
    min-width: 31px;
    padding:11px 9px 11px 10px;
    &:first-child {
      width: 61%;
    }
    &:last-child {
      padding-right: 9px;
    }
  }
  th{
    color: $cart-text-color;
    font-size: 12px;
    padding-top: 6px;
    padding-bottom: 6px;
    vertical-align: top;
    &:first-child {
      font-size: 14px;
    }
  }
  td{
	
    @media only screen and (min-width: 750px) {}
  }
}

.cart__row{
  border-bottom: 2px solid $cart-border-color;
}

.cart__product-information{
/*   display: flex; */
  flex-direction: row;
  flex-wrap: nowrap;
  justify-content: flex-start;
  align-content: center;
  align-items: center;
}

.cart__image-wrapper{
  @media only screen and (min-width: 750px) {}
  @media only screen and (min-width: 990px) {
    flex: 0 1 auto;
    max-width: 100px;
    min-width: 100px;
    padding: 0 10px;
  }
}

.list-view-item__title{
  color: $cart-text-color;
  font: 600 16px/1.5 'GT America', sans-serif;
  letter-spacing: 1.33px;
  text-transform: uppercase;
}

.template-cart {
  .list-view-item__title{
    a{
      &.cart__product-title {
        border: 0;
        color: inherit;
        font: inherit;
        letter-spacing: inherit;
        text-decoration: none;
        text-transform: inherit;
      }
    }
  }
}

.product-details{
  color: $cart-text-color;
  display: flex;
  flex-direction: row;
  flex-wrap: wrap;
  justify-content: flex-start;
  align-content: flex-start;
  align-items: flex-start;
  font-weight: 100;
  font-size: 14px;
  margin: 0;
  padding: 0;
}

.product-details__item{
  line-height: inherit;
  margin: 0 10px 0 0;
}

.cart__qty{
  display: inline-block;
  position: relative;
  .dec,
  .inc{
    cursor: pointer;
    font-size: 14px;
    padding: 5px;
    position: absolute;
    top: 50%;
    transform: translateY(-50%);
    z-index: 5;
    &:hover {
      color: $color-theme-primary;
    }
  }
  .dec{
    left: 20px;
  }
  .inc{
    right: 20px;
  }
  input{
    border-color:$cart-text-color;
  	border-radius:0;
  }
}

.cart__qty-input{
  width: 100px;
}

.cart__remove{
  display: inline-block;
  vertical-align: middle;
  font-size: 0;
  line-height: 0;
  margin: 0;
  a{
    border: 0;
    display: inline-block;
    vertical-align: middle;
    font-size: 0;
    line-height: 0;
    padding: 5px;
    text-decoration: none;
    width: 22px;
    &:hover {
      svg{
        fill: $color-theme-primary;
      }
    }
    svg{
      fill: $cart-text-color;
      width: 100%;
    }
  }
}

.cart__footer{
  padding: 5px 0 0;
  .grid{
    margin: 0;
  }
  .grid__item{
    padding-left: 0;
  }
  .cart-summary{
    margin: 0 0 7px;
    .title{
      display: block;
      font-size: 12px;
      font-weight: normal;
      margin: 0 0 10px;
    }
    ul{
      border: solid $cart-text-color;
      border-width: 2px 0;
      padding: 14px 8px 10px 0;
      li{
        display: flex;
        flex-direction: row;
        flex-wrap: nowrap;
        justify-content: space-between;
        align-content: flex-start;
        align-items: flex-start;
        padding: 0 0 10px;
      }
    }
  }
}

.cart-subtotal{
  color: $cart-text-color;
  display: flex;
  flex-direction: row;
  flex-wrap: nowrap;
  justify-content: space-between;
  align-content: flex-start;
  align-items: flex-start;
  font-size: 16px;
  font-weight: bold;
  margin: 0 0 29px;
  .cart-subtotal__title{
    font-size: inherit;
  }
  .cart-subtotal__price{
    min-width: 0;
    padding-left: 15px;
  }
}

.cart-notes{
  padding: 0 0 11px;
  .form-control{
    padding: 0 0 18px;
  }
  textarea{
    font-size: 16px;
    line-height: normal;
    height: 71px;
    min-height: 71px;
    padding: 10px 14px;
    resize: vertical;
    width: 100%;
  }
  .label-checkbox{
    padding: 0 0 0 32px;
    position: relative;
    input{
      left: 0;
      opacity: 0;
      position: absolute;
      top: 0;
      &:checked {
        ~ .fake-checkbox{
          &:before {
            display: block;
          }
        }
      }
    }
    .fake-checkbox{
      border: 1px solid $cart-text-color;
      height: 21px;
      left: 0;
      position: absolute;
      top: 1px;
      width: 21px;
      &:before {
        $gap: 3px;
        background: $color-theme-primary;
        bottom: $gap;
        content: '';
        display: none;
        left: $gap;
        position: absolute;
        right: $gap;
        top: $gap;
      }
    }
    a{
      color: $color-theme-primary;
      font-size: inherit;
      font-weight: inherit;
      letter-spacing: 0;
      line-height: inherit;
      text-decoration: none;
      text-transform: none;
      &:hover {
        color: $cart-text-color
      }
    }
  }
}