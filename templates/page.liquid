{% include 'page-header' %}
<div class="page-width2">
  <h1 class="page-title">{{ page_title }}</h1>
</div>
<div class="page-width2 page-wrapper">
  <div class="rte">
    {{ page.content }}
  </div>
</div>
